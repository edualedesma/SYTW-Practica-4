%ol
  - @list.each do |url|
    %li
      #{url.url}
      - if (url.myurl == '')
        %a{:href=>"/#{url.id.to_s(Base)}"} (#{url.id.to_s(Base)})
      - else
        %a{:href=>"/#{url.myurl}"} (#{url.myurl})